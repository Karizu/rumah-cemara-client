<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center">

    <ImageView
        android:id="@+id/iv_carousel"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scaleType="centerCrop"
        android:src="@color/FocusColorText" />

    <TextView
        android:id="@+id/tv_label_carousel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="@dimen/margin_padding_size_large"
        android:layout_marginRight="@dimen/margin_padding_size_large"
        android:layout_marginBottom="64dp"
        android:text="label"
        android:textColor="@color/White"
        android:textSize="@dimen/text_size_large" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_gravity="right"
        android:layout_margin="@dimen/margin_padding_size_medium"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iv_carousel_reminder"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginRight="@dimen/margin_padding_size_medium"
            android:contentDescription="@string/reminder_description"
            android:src="@drawable/icon_bell" />

        <ImageView
            android:id="@+id/iv_carousel_share"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:contentDescription="@string/share_description"
            android:src="@drawable/icon_share" />
    </LinearLayout>

</RelativeLayout>